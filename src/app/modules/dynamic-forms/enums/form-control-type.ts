export enum FormControlType {
  CHECKBOX = 'checkbox',
  CHIPS = 'chips',
  DATE_RANGE = 'date-range',
  DATE = 'date',
  DROPDOWN = 'dropdown',
  RADIO = 'radio',
  SLIDE = 'slide',
  TEXT = 'text',
  TEXT_AREA = 'textarea',
}